{
  "name": "animakit",
  "private": true,
  "description": "ANIMA — Agentic Neuro-Inspired Memory Architecture. Battle-tested cognitive architecture for LLM agents, extracted from 53 sprints in production.",
  "license": "MIT",
  "author": "Justine Serna Aza",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.2.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "bench": "pnpm -r bench",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "@vitest/coverage-v8": "^3.2.6",
    "tsup": "^8.4.0",
    "tsx": "^4.22.4",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
